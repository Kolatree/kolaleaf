// KolaleafWidgets.swift  (Phase 10A · U70)
// Widget Extension @main bundle. Hosts the TransferLiveActivity
// configured in `TransferLiveActivity.swift`.

import WidgetKit
import SwiftUI

@main
struct KolaleafWidgetsBundle: WidgetBundle {
    var body: some Widget {
        TransferLiveActivity()
    }
}
